# {{ ade_name }}

> {{ description }}

## What is this?

This is an **ADE (Agent Dev Environment)** — a Cursor workspace scaffolded from a [copier template](https://github.com/madi-parloa/ade-template). It contains:

- A portfolio of Parloa infrastructure repos (cloned by `install.sh`).
- GSD (Get Shit Done) installed workspace-locally for spec-driven development.
- Kitchen skills and plugins from `claudes-kitchen` and `open-kitchen`.
- Pre-seeded codebase intelligence in `.planning/codebase/` so GSD is immediately Parloa-aware.

## Quick start

This ADE was created with:

```bash
uvx copier copy git+https://github.com/madi-parloa/ade-template.git <this-directory>
```

To open it:

```bash
cursor .
```

## Re-running the installer

If you need to re-clone missing repos or re-install GSD/kitchens:

```bash
./install.sh
```

The script is idempotent — it skips repos that already exist.

## Updating from the template

When the template improves (new kitchen, updated install logic, refreshed codebase intel):

```bash
uvx copier update
```

Copier re-renders template files (preserving your answers) and re-runs `install.sh`.

## Producing a different ADE

1. **Edit in place.** Change `portfolio.txt` and `install.sh` in this directory. Re-run `./install.sh`.
2. **Fork the template.** `gh repo fork madi-parloa/ade-template --remote` and scaffold from your fork.
3. **Answer copier questions differently.** Run `uvx copier copy ...` again with different answers.

## Files

| File | Purpose |
|------|---------|
| `AGENTS.md` | Agent-level workspace context (what repos are here, conventions, guardrails) |
| `CLAUDE.md` | Claude-specific instructions |
| `portfolio.txt` | List of git URLs to clone |
| `install.sh` | Clones repos, installs GSD + kitchens |
| `.planning/PROJECT.md` | GSD project definition |
| `.planning/codebase/*.md` | Pre-seeded codebase intelligence |
| `.cursor/` | Workspace-local Cursor config (populated by `install.sh`) |
